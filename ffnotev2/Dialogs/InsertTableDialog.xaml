<Window x:Class="ffnotev2.Dialogs.InsertTableDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="표 삽입" Height="180" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        FocusManager.FocusedElement="{Binding ElementName=RowsBox}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Margin="0,0,6,0">
                <TextBlock Text="행 (데이터)" Margin="0,0,0,4"/>
                <TextBox x:Name="RowsBox" Text="3" Padding="4"
                         PreviewTextInput="Numeric_PreviewTextInput"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Margin="6,0,0,0">
                <TextBlock Text="열" Margin="0,0,0,4"/>
                <TextBox x:Name="ColsBox" Text="3" Padding="4"
                         PreviewTextInput="Numeric_PreviewTextInput"/>
            </StackPanel>
        </Grid>
        <TextBlock Grid.Row="1" Text="헤더 행 1개가 자동 추가됩니다." Margin="0,8,0,0"
                   Foreground="#FF666666" FontSize="11"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="삽입" Width="80" Margin="0,0,8,0" Click="Confirm_Click" IsDefault="True"/>
            <Button Content="취소" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
